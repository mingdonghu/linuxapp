#!/bin/bash
# "# 单行注释"
# 多行注释
:<<!
#! 是一个约定标记，指定这个脚本使用哪个shell解释器执行。这里指明使用bash解释器，这也是最常用的shell解释器。
常见的shell解释器还有：
Bourne Shell（/usr/bin/sh或/bin/sh）
C Shell（/usr/bin/csh）
K Shell（/usr/bin/ksh）
!

echo "hello shell"
